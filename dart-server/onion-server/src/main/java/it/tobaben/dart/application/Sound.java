package it.tobaben.dart.application;

/**
 * Sounds the clients can play. Names identical to the anthrax Sounds enum;
 * published as {"sound": "<name>"} on status/playSound (see root README).
 */
public enum Sound {
    TREFFER,
    DOUBLE,
    TRIPLE,
    BULLSEYE,
    WINNER,
    STRAFE,
    UEBERWORFEN,
    RESET,
    SPIELSTART,
    MAXPOINTS
}
